table:
  name: auth_user
  schema: public
array_relationships:
  - name: sessions
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: auth_session
          schema: public
